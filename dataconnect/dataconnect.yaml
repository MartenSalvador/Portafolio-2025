specVersion: "v1"
serviceId: "portafolio"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "portafolio-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
